[**McpAdapter**](../../README.md)

***

[McpAdapter](../../README.md) / [declarations](../README.md) / McpToolHandler

# Type Alias: McpToolHandler

> **McpToolHandler** = (`args`, `event`) => `Promiseable`\<`unknown`\>

A tool handler: receives the validated arguments (and the underlying event) and returns a
result. The result is serialised into MCP tool content.

## Parameters

### args

`Record`\<`string`, `unknown`\>

### event

`IncomingEvent`

## Returns

`Promiseable`\<`unknown`\>
