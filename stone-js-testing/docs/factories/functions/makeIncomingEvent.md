[**Testing**](../../README.md)

***

[Testing](../../README.md) / [factories](../README.md) / makeIncomingEvent

# Function: makeIncomingEvent()

> **makeIncomingEvent**(`metadata?`): `IncomingEvent`

Builds a generic (platform-agnostic) `IncomingEvent` for tests.

## Parameters

### metadata?

`Record`\<`string`, `unknown`\> = `{}`

The event metadata (accessible via `event.get`).

## Returns

`IncomingEvent`

The incoming event.
