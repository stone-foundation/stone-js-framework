[**Auth**](../../../README.md)

***

[Auth](../../../README.md) / [middleware/guards](../README.md) / normalizeScopes

# Function: normalizeScopes()

> **normalizeScopes**(`scope`): `string`[]

Normalises a `scope` claim (space-delimited string or array) into an array.

## Parameters

### scope

`unknown`

The raw scope claim.

## Returns

`string`[]

The scopes.
