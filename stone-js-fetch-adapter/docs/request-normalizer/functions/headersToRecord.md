[**FetchAdapter**](../../README.md)

***

[FetchAdapter](../../README.md) / [request-normalizer](../README.md) / headersToRecord

# Function: headersToRecord()

> **headersToRecord**(`headers`): `Record`\<`string`, `string`\>

Turns a `Headers` object into a plain lower-cased record.

## Parameters

### headers

`Headers`

The Web headers.

## Returns

`Record`\<`string`, `string`\>

A plain record.
