/**
 * The platform identifier for the Node.js WebSocket adapter.
 *
 * Used as the adapter alias/platform within the Stone.js framework, so the blueprint middleware and
 * resolvers can target this adapter specifically.
 */
export const NODE_WS_PLATFORM = 'node_ws'
