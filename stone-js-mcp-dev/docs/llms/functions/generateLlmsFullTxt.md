[**Mcp Dev**](../../README.md)

***

[Mcp Dev](../../README.md) / [llms](../README.md) / generateLlmsFullTxt

# Function: generateLlmsFullTxt()

> **generateLlmsFullTxt**(`base?`): `string`

Generates the fuller `llms-full.txt` (adds best-practices and known gaps) — the complete brief
for an agent building with Stone.js.

## Parameters

### base?

[`KnowledgeBase`](../../declarations/interfaces/KnowledgeBase.md) = `knowledgeBase`

The knowledge base (defaults to the built-in one).

## Returns

`string`

The `llms-full.txt` content.
