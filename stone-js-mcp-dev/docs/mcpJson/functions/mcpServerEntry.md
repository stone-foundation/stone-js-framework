[**Mcp Dev**](../../README.md)

***

[Mcp Dev](../../README.md) / [mcpJson](../README.md) / mcpServerEntry

# Function: mcpServerEntry()

> **mcpServerEntry**(`command?`): `object`

The `.mcp.json` server entry that launches this dev server.

## Parameters

### command?

`string` = `'stone'`

The launcher command (defaults to `stone`).

## Returns

`object`

The MCP server entry.

### args

> **args**: `string`[]

### command

> **command**: `string`
