# Public by construction: these values are compiled into the browser bundle.
# The smoke gate asserts the generated script is linked, so this file is a fixture, not a secret.
APP_PUBLIC_MARKER=smoke-ok
