[**Resources**](../../README.md)

***

[Resources](../../README.md) / [helpers](../README.md) / stripUndefined

# Function: stripUndefined()

> **stripUndefined**\<`T`\>(`object`): `Partial`\<`T`\>

Returns a copy of `object` without any `undefined` values (so conditional fields simply vanish).

## Type Parameters

### T

`T` *extends* [`ResourceOutput`](../../declarations/type-aliases/ResourceOutput.md)

## Parameters

### object

`T`

The object to clean.

## Returns

`Partial`\<`T`\>

A copy without undefined values.
