# Variable: STONE\_LOGO

```ts
const STONE_LOGO: string[];
```

The Stone.js gem logo: a faceted diamond built from the signature mark, drawn above the wordmark.
Pure ASCII (mark + spaces) so it stays testable and terminal-safe.
