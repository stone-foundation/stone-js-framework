# Function: toSegments()

```ts
function toSegments(key): PropertyKey[];
```

Normalize a key into path segments.

## Parameters

### key

`PropertyKey` \| `PropertyKey`[]

A dotted string, an array of segments, or a single symbol/number/string.

## Returns

`PropertyKey`[]

The path segments.
