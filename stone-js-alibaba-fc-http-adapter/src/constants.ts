/**
 * Platform identifier for the Alibaba Cloud Function Compute HTTP adapter.
 *
 * Used to key the adapter in the blueprint and to tag the event source, so the rest of the
 * framework can recognise an Alibaba FC HTTP-triggered request.
 */
export const ALIBABA_FC_HTTP_PLATFORM = 'alibaba_fc_http'
