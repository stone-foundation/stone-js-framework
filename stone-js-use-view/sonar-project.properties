sonar.projectName=Stone.js - Use View
sonar.organization=stone-foundation
sonar.projectKey=stone-foundation_stone-js-use-view
sonar.coverage.exclusions=**/*.spec.ts, **/*.test.ts
sonar.javascript.lcov.reportPaths=./coverage/lcov.info
sonar.projectDescription=Stone.js Use View is the framework-agnostic view engine layer: page/head contracts, rendering modes, XSS-safe SSR snapshots, server data-fetching, and the ViewEngine interface shared by every view integration.

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=use-view
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
